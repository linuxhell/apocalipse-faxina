[package]
name = "apocalipse-faxina"
version = "0.2.0"
edition = "2021"
authors = ["linuxhell"]
description = "Apocalipse Faxina - manutenção e limpeza portátil para Windows x64"
build = "build.rs"

[dependencies]
eframe = { version = "0.31.1", default-features = false, features = ["glow", "default_fonts"] }
image = { version = "0.25.6", default-features = false, features = ["jpeg"] }
rodio = { version = "0.20.1", default-features = false, features = ["symphonia-aac", "symphonia-isomp4"] }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = [
  "Win32_Foundation",
  "Win32_UI_WindowsAndMessaging"
] }

[build-dependencies]
winresource = "0.1"

[profile.release]
opt-level = "z"
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
